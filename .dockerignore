# We don't need .git directory as of now as we don't run any `git`
# commands or need any git related details as part of the docker build
.git

# We don't need helm chart directory
helm-chart/

docker-compose.yml

.gitignore

CONTRIBUTING.md

.github

helm-values.yaml

Dockerfile

Dockerfile.debug

.vscode/

README.md
